# Filtrer les trajets par boucle

Filtrer les trajets par boucle

## Usage

``` r
filtrer_trajet(trajet, boucle)
```

## Arguments

- trajet:

  Un data.frame de données (ex: df_velo).

- boucle:

  Un vecteur de numéros de boucles.

## Value

Le data.frame filtré.
